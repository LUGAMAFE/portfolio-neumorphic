export type Styles = {
  ConfigurationRange__input: string;
  ConfigurationRange__label: string;
  ConfigurationRange__row: string;
};

export type ClassNames = keyof Styles;

declare const styles: Styles;

export default styles;
